# HERD dependencies — ranges, not pins (BLOCK 5).
# Resolve: pip install -r requirements.in -> pip check -> tools/smoke_deps.py
#          -> pip freeze > requirements.lock.txt

# --- API ---
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
python-multipart>=0.0.9
websockets>=12.0

# --- Types ---
pydantic>=2.8,<3.0
pydantic-settings>=2.4,<3.0

# --- Storage ---
SQLAlchemy>=2.0,<2.1
aiosqlite>=0.20
greenlet>=3.0

# --- LLM (google-genai, NOT the deprecated google-generativeai) ---
google-genai>=1.0

# --- Orchestration ---
langgraph>=0.2,<1.0

# --- Embeddings / vector ---
chromadb>=0.5,<2.0

# --- Images ---
Pillow>=10.0
ImageHash>=4.3

# --- Stats ---
numpy>=1.26,<3.0
scipy>=1.11

# --- Ids / logs / http ---
python-ulid>=2.2
structlog>=24.1
httpx>=0.27
python-dotenv>=1.0
PyYAML>=6.0

# --- Telegram (polling, not webhook) ---
python-telegram-bot>=21.0

# --- Crawl / parse ---
beautifulsoup4>=4.12
lxml>=5.0

# --- Test ---
pytest>=8.0
pytest-asyncio>=0.23
pytest-timeout>=2.3
